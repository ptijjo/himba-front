# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Project rules (métier, sécurité, contrat API) live in `.cursor/rules/` — that folder is the source of truth for everything except the Expo SDK version pointer above.
